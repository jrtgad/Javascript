function checkHour(name) {
    var TODAY = new Date(),
        HOUR = today.getHours(),        

        TEXT = HOUR < 12 ? "Buenos días " : HOUR < 21 ? "Buenas tardes " : "Buenas noches ";
    TEXT += name;
    return text;
}